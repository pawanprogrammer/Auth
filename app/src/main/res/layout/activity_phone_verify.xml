<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PhoneOTP.PhoneVerifyActivity">

    <EditText
        android:id="@+id/phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Mobile No."
        android:inputType="phone"
        android:maxLength="10"
        app:layout_constraintTop_toTopOf="parent"></EditText>

    <EditText
        android:id="@+id/otp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter OTP"
        app:layout_constraintTop_toBottomOf="@+id/phone"></EditText>

    <Button
        android:id="@+id/generate_otp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Generate OTP"
        app:layout_constraintTop_toBottomOf="@+id/otp"></Button>

    <Button
        android:id="@+id/verify_otp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Verify OTP"
        app:layout_constraintTop_toBottomOf="@+id/generate_otp"></Button>
</androidx.constraintlayout.widget.ConstraintLayout>